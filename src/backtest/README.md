# src/backtest

Simulation engine and evaluation logic to measure strategy performance over time.

- `engine.py` consumes engineered returns plus optimized weights to generate cumulative performance curves in the gold layer.
