# src/features

Feature engineering tasks responsible for transforming silver inputs into model-ready factors.

- `normalize_esg.py` converts silver ESG scores into percentile and min-max features.
- `make_returns_cov.py` derives daily returns and rolling covariance matrices from silver prices.
