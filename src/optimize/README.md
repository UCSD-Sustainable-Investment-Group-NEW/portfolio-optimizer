# src/optimize

Portfolio construction and efficient frontier solvers built atop the engineered feature set.

- `frontier.py` solves a long-only, capped-weight mean-variance problem using the latest feature returns and covariances.
